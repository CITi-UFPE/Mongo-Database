{
  "name": "citi-data-platform",
  "private": true,
  "scripts": {
    "dev": "npm run dev --workspace=client",
    "build": "npm run build --workspace=client"
  },
  "workspaces": [
    "client"
  ],
  "devDependencies": {
    "mkdirp": "^3.0.1",
    "rimraf": "^6.1.0"
  }
}
